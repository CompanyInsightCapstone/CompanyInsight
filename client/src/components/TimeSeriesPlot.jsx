import * as d3 from 'd3';


export default function TimeSeriesPlot({timeSeriesData}) {
    const {data, width, height, xScale, yScale, colorScale, xValue, yValue, colorValue, title, xAxisLabel, yAxisLabel } = timeSeriesData;

    return (
        <p>placeholder</p>
    )
}
